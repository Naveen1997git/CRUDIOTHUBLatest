# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- dev

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  azureSubscription: 'AzureConnection-MyIotApp'
  acrServiceConnection: 'ACRConnection-MyIotApp-New'
  acrName: 'myiotacr'
  appName: 'myiotapp'
  imageName: 'myiotapp'
  packagePath: '$(Build.ArtifactStagingDirectory)/WebApp.zip'
  aksResourceGroup: 'iotthubcrud'
  aksClusterName: 'myiot-aks'

steps:
# Install NuGet
- task: NuGetToolInstaller@1

# Restore NuGet packages
- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

# Build and publish the ASP.NET app
- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArgs: >
      /p:DeployOnBuild=true
      /p:WebPublishMethod=Package
      /p:PackageAsSingleFile=true
      /p:SkipInvalidConfigurations=true
      /p:DesktopBuildPackageLocation=$(Build.ArtifactStagingDirectory)\WebApp.zip
      /p:DeployIisAppPath="Default Web Site"
      /p:PublishDir=$(Build.SourcesDirectory)\publish\
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

# Run unit tests
- task: VSTest@2
  inputs:
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

# Publish build artifacts
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'

# Deploy to Azure App Service
- task: AzureRmWebAppDeployment@4
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: '$(azureSubscription)'
    appType: 'webApp'
    WebAppName: '$(appName)'
    packageForLinux: '$(packagePath)'

# ✅ Optional: Verify contents of publish folder
- powershell: |
    Write-Host "Listing contents of publish folder:"
    Get-ChildItem -Path "$(Build.SourcesDirectory)\publish" -Recurse
  displayName: 'Verify publish folder contents'

# ✅ Docker build and push with corrected context
- task: Docker@2
  inputs:
    containerRegistry: '$(acrServiceConnection)'
    repository: '$(imageName)'
    command: 'buildAndPush'
    Dockerfile: 'Dockerfile'
    buildContext: '$(Build.SourcesDirectory)\publish'
    tags: |
      latest

# Deploy to AKS
- task: Kubernetes@1
  inputs:
    connectionType: 'Azure Resource Manager'
    azureSubscription: '$(azureSubscription)'
    azureResourceGroup: '$(aksResourceGroup)'
    kubernetesCluster: '$(aksClusterName)'
    namespace: 'default'
    command: apply
    useConfigurationFile: true
    configuration: 'manifests/deployment.yaml'
